[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "culture-compass"
version = "0.1.0"
description = "Cultural event recommendation system"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["culture_compass"]